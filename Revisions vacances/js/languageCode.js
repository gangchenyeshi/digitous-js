

//03 LANGUAGE CODE
// Create a `secretCode` function which takes a string as a parameter and which replaces each letter with their order of appearance in the alphabet + 10.
// `A` becomes` 11`, `B` becomes` 12`, etc.
// Return the result as a string.
//```jsx
//console.log(secretCode("secret")); // affichera 291513281530

function secretCode(string) {
    var numberCode =" ";
    var splitString = string.split('');
    console.log(splitString);
    for (var i= 1; i<=splitString.length; i++) {
        
    }
}
secretCode("secret")
